# ─── Database ───
# Supabase: Dashboard → Settings → Database → Connection string (URI)
# Use the "Transaction" pooler (port 6543) for serverless, "Session" (port 5432) for long-lived
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# ─── Auth ───
SESSION_SECRET=change-me-to-random-string

# ─── Encryption ───
# 32-byte hex key for AES-256-GCM token encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If omitted, derives a key from SESSION_SECRET (fine for dev)
ENCRYPTION_KEY=

# ─── YouTube OAuth ───
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0
# Enable "YouTube Data API v3" under APIs & Services → Library
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REDIRECT_URI=http://localhost:3001/oauth/youtube/callback

# ─── TikTok OAuth ───
# TikTok Developer Portal → Manage Apps → Login Kit
# Sandbox mode works without review (limited scope)
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
TIKTOK_REDIRECT_URI=http://localhost:3001/oauth/tiktok/callback

# ─── CRE ───
# Shared secret between backend and CRE workflow
# Note: env var name MUST differ from secret name in secrets.yaml
CRE_SERVICE_TOKEN=your-cre-service-token

# ─── Mock mode ───
# Set to "true" to return fake metrics (for testing without OAuth)
USE_MOCK_METRICS=false

# ─── Frontend ───
FRONTEND_URL=http://localhost:3000

# ─── Blockchain ───
DEPLOYER_PRIVATE_KEY_ENV=0x_your_private_key
BASE_SEPOLIA_RPC=https://sepolia.base.org
USDC_ADDRESS=0x036CbD53842c5426634e7929541eC2318f3dCF7e
CONTRACT_ADDRESS=0x56A512A9Dfa24F0Bb823a782afe3b658627C6496
